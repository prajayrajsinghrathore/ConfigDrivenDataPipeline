# >=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>==
# ConfigDrivenDataPipeline Requirements
# >=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>==
# Requires Python >=3.10,
# >=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>==

# Foundation dependencies (install first to avoid conflicts)
charset-normalizer>=3.4.7
six>=1.17.0
python-dateutil>=2.9.0
pytz>=2025.2
dnspython>=2.8.0
pyparsing>=3.3.2
filelock>=3.20.3
asn1crypto>=1.5.1
rsa>=4.9.1
pynacl>=1.5.0
bcrypt>=5.0.0
invoke>=2.2.1

# HTTP/2 protocol stack (install together)
hyperframe>=6.1.0
hpack>=4.1.0
h2>=4.3.0
httpx>=0.28.1

# Protocol buffers and serialization (before frameworks that use them)
proto-plus>=1.27.0
orjson>=3.11.7
protobuf>=6.33.5
multidict>=6.7.1
overrides >=7.7.0

# Microsoft Kiota libraries - NOT NEEDED (MS Graph SDK)
# microsoft-kiota-abstractions>=1.9.8
# microsoft-kiota-authentication-azure>=1.9.8
# microsoft-kiota-http>=1.9.8


# Crypto and Security (MUST be early to avoid version conflicts)
# Note: cryptography<46 required by apache-airflow-providers-cncf-kubernetes
cryptography>=48.0.1
pyjwt>=2.11.0
# pyOpenSSL removed in Airflow 3.2.0
platformdirs>=4.10.0

# Structured Logging (Airflow 3.1.6 recommendation)
structlog>=24.1.0

# Timezone handling (Airflow 3.1.6 requirement for timezone-aware datetimes)
pendulum>=3.0.0

# Core numerical and data processing (heavy compilation)

# Web and parsing libraries
requests>=2.32.5
lxml>=6.0.2

# Database connectivity
# NOTE: Don't override SQLAlchemy - Airflow 3.1.6 requires compatible version from base image
# Note: paramiko<4.0.0 required by apache-airflow-providers-ssh
paramiko>=2.9.0
#redis>=7.1.0
tenacity>=9.1.2

# Azure identity (before other Azure packages)
azure-core>=1.38.0
# azure-datalake-store>=1.0.1  # Legacy Gen1 - NOT NEEDED (using Gen2)
msal>=1.34.0
azure-identity>=1.25.1
azure-storage-blob>=12.28.0
adlfs

# adal>=1.2.7  # Deprecated - replaced by MSAL
# azure-batch>=14.2.0  # NOT NEEDED - no batch jobs
# azure-cosmos>=4.9.0  # NOT NEEDED - no CosmosDB
# azure-keyvault-secrets>=4.10.0  # NOT NEEDED - uncomment if using Key Vault
# azure-kusto-data>=6.0.1  # NOT NEEDED - no Kusto/ADX
# azure-mgmt-containerinstance>=10.1.0  # NOT NEEDED - management plane
# azure-mgmt-containerregistry>=14.0.0  # NOT NEEDED - management plane
# azure-mgmt-cosmosdb>=9.8.0  # NOT NEEDED - management plane
# azure-mgmt-datafactory>=9.2.0  # NOT NEEDED - management plane
# azure-mgmt-datalake-store>=0.5.0  # NOT NEEDED - management plane
# azure-mgmt-resource>=24.0.0  # NOT NEEDED - management plane
# azure-mgmt-storage>=24.0.0  # NOT NEEDED - management plane
# azure-servicebus>=7.14.3  # NOT NEEDED - no Service Bus
azure-storage-file-datalake>=12.20.0
# azure-storage-file-share>=12.21.0  # NOT NEEDED - no File Share
# azure-synapse-artifacts>=0.20.0  # NOT NEEDED - no Synapse
# azure-synapse-spark>=0.7.0  # NOT NEEDED - no Synapse
# msgraph-core>=1.3.8  # NOT NEEDED - no MS Graph API

# Heavy data processing frameworks (install after dependencies)
snowflake-connector-python>=4.2.0
#apache-beam[azure]>=2.71.0

# Airflow (install after all dependencies)
apache-airflow-task-sdk==1.3.0
apache-airflow-core==3.3.0
apache-airflow==3.3.0

# Airflow providers (install last - depends on many packages above)
apache-airflow-providers-microsoft-azure==13.5.0
apache-airflow-providers-cncf-kubernetes==10.19.0
apache-airflow-providers-snowflake==6.14.0
apache-airflow-providers-ftp==3.15.2
apache-airflow-providers-http==6.0.4
apache-airflow-providers-ssh==5.0.4
apache-airflow-providers-apache-kafka==1.14.0
cerberus>=1.3.8

# Formula Engine dependencies
simpleeval>=1.0.3
numexpr>=2.14.1

# Data Quality - Soda Core
# soda-core-snowflake>=3.3.0

# Data Lineage - OpenLineage
# NOTE: openlineage-sql requires Rust compilation on Windows
# ZScaler blocks rustup download. Works fine in Docker (Linux).
# Comment out for local Windows development, uncomment for Docker builds.
apache-airflow-providers-openlineage==2.18.1

# Kafka requirements
kafka-python>=2.3.0
confluent-kafka>=2.13.0
avro>=1.12.0  # Replaces deprecated avro-python3

# >=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>==
# TESTING DEPENDENCIES
# >=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>=>==

# Core testing framework
pytest>=9.0.2
pytest-cov>=7.0.0
pytest-timeout>=2.4.0
pytest-mock>=3.15.0
pytest-asyncio>=1.3.0

# Test utilities
freezegun>=1.5.1
responses>=0.25.3
requests-mock>=1.12.1
httpretty>=1.1.4

# Schema validation for contract tests
jsonschema>=4.26.0

# Database testing
psycopg2-binary>=2.9.10

# Test containers (for isolated integration tests)
# testcontainers>=4.8.0  # Optional: uncomment for Docker-based testing

# Code quality
black>=24.10.0
isort>=5.13.2
flake8>=7.1.1
mypy>=1.13.0

# Type stubs
types-requests>=2.32.0.20241016
types-PyYAML>=6.0.12.20240917
asyncpg

apache-airflow-providers-fab==3.7.1
apache-airflow-providers-celery==3.21.0
